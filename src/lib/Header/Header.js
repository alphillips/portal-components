import React from "react";

import './header.css'

const styles = {
  fontWeight:'800'
}

const Header = () => (
  <div className={'header-main'} style={styles}>
    This is the header!!
  </div>
);

export default Header;
